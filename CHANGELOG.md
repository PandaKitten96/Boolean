#### Unreleased

* Align contribution guidance to the `master` branch.
* Add CI test execution and an HLint check job.
* Replace the broken Makefile include with local `build`, `test`, `haddock`, and `clean` targets.
* Add a minimal `cabal.project` for local development.
* Align `tested-with` entries with the current CI matrix.
* Update Stack resolver to `lts-21.25`.
* Add a pull request template.

#### 0.2.4.1

* Bump `cabal-version` to `≥ 1.10`.
* Tested with GHC 7.0 - 9.6.
